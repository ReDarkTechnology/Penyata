using System;
using SDL2;

namespace Penyata
{
	public class BoxCollider : Collider
	{
		/*public override void GizmoUpdate(bool overrideHide = false)
		{
			if(drawColliderRect || overrideHide)
			{
				Color color = Color.green;
		    	SDL.SDL_SetRenderDrawColor(Main.renderer, color.byteR, color.byteG, color.byteG, color.byteA);
				var rect = new SDL.SDL_Rect() {
					x = Convert.ToInt32(transform.drawRect.x),
					y = Convert.ToInt32(transform.drawRect.y),
					w = Convert.ToInt32(transform.drawRect.w * size.x),
					h = Convert.ToInt32(transform.drawRect.h * size.y)
				};
				SDL.SDL_RenderDrawRect(Main.renderer, ref rect);
			}
		}*/
	}
}
